import 'package:flutter/material.dart';

class Suaconta extends StatelessWidget {
  const Suaconta({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Sua conta')),
    );
  }
}
