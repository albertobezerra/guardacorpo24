import 'package:flutter/material.dart';

class DdsRaiz extends StatelessWidget {
  const DdsRaiz({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
